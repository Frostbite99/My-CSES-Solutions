#include <iostream>
#include <vector>

using namespace std;
/*
int main() {
	
	int boundary_value = 0;
	cin >> boundary_value;

	if (boundary_value == 1) {
		cout << boundary_value;
	}

	else if (boundary_value == 4) {
		cout << "2 4 1 3";
	}

	else if (boundary_value < 5) {
		cout << "NO SOLUTION";
	}

	else {
		for (int i = 1; i <= boundary_value; i++) {
			if (i % 2) {
				cout << i << " ";
			}
		}

		for (int i = 1; i <= boundary_value; i++) {
			if (!(i % 2)) {
				cout << i << " ";
			}
		}
	}
	
	
	return 0;
}*/