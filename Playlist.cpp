#include <bits/stdc++.h>

using namespace std;

/*
int main() {
	int number_of_inputs = 0;
	int entry = 0; int exit = 0;
	int cur = 0;
	int answer = 0;
	map<int, int> musteri_map;
	cin >> number_of_inputs;
	while (number_of_inputs--) {
		cin >> entry;
		cin >> exit;
		musteri_map[entry]++;
		musteri_map[exit]--;
	}
	

	for (auto& x : musteri_map) {
		cur += x.second;
		answer = max(answer, cur);
	}

	cout << answer;

	return 0;
}
*/