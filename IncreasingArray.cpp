#include <iostream>
#include <vector>

using namespace std;
/*
int main() {
	vector<int> element_array;
	int dummy = 0;
	long counter = 0;
	int number_of_elements = 0;
	cin >> number_of_elements;

	for (int i = 0; i < number_of_elements; i++) {
		cin >> dummy;
		element_array.push_back(dummy);
	}

	for (int i = 0; i < element_array.size()-1; i++) {
		while (element_array[i + 1] < element_array[i]) {
			++element_array[i + 1];
			counter++;
		}
	}
	
	cout << counter;
	
	return 0;
}
*/